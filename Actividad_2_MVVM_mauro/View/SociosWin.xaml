<Window x:Class="Actividad_2_MVVM_mauro.SociosWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Actividad_2_MVVM_mauro" xmlns:centrodepoertivoviewmodel="clr-namespace:CentroDepoertivoViewModel" d:DataContext="{d:DesignInstance Type=centrodepoertivoviewmodel:SocioViewModel}"
        mc:Ignorable="d"
        Title="SociosWin" Height="450" Width="800">
    <Grid Background="#E9EDF2">

        <!-- Contenedor principal -->
        <Border Background="#FFFFFF"
                CornerRadius="12"
                Padding="20"
                Margin="20">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Título -->
                <TextBlock Text="Gestión de Socios"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#1F2937"
                           Margin="5,0,0,15"/>

                <!-- Formulario -->
                <Border Grid.Row="1"
                        Background="#F5F7FA"
                        CornerRadius="8"
                        Padding="15"
                        Margin="0,0,0,15">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Nombre"
                                   Grid.Row="0"
                                   Grid.Column="0"
                                   Margin="0,0,5,8"/>

                        <TextBox x:Name="txtNombre"
                                 Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"
                                 Grid.Row="0"
                                 Grid.Column="1"
                                 Height="26"
                                 Margin="0,0,0,8"/>

                        <TextBlock Text="Email"
                                   Grid.Row="0"
                                   Grid.Column="3"
                                   Margin="0,0,5,8"/>

                        <TextBox x:Name="txtEmail"
                                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                 Grid.Row="0"
                                 Grid.Column="4"
                                 Height="26"
                                 Margin="0,0,0,8"/>

                        <TextBlock Text="Activo"
                                   Grid.Row="1"
                                   Grid.Column="0"/>

                        <ComboBox x:Name="cbActivo"
                                  SelectedValue="{Binding Activo}"
                                  Grid.Row="1"
                                  Grid.Column="1"
                                  Height="26"
                                  IsEditable="False"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="True"/>
                            <ComboBoxItem Content="False"/>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- DataGrid -->
                <DataGrid x:Name="dgSocios"
                          ItemsSource="{Binding Socios}"
                          SelectedItem="{Binding SelectedSocio, Mode=TwoWay}"
                          Grid.Row="2"
                          AutoGenerateColumns="True"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          Margin="0,0,0,15"
                          SelectionMode="Single"/>

                <!-- Botones -->
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnAñadir"
                            Command="{Binding AgregarSocio}"
                            Grid.Column="0"
                            Content="Añadir"
                            Height="38"
                            Background="#D1FAE5"
                            BorderBrush="#10B981"
                            Margin="5"/>

                    <Button x:Name="btnModificar"
                            Command="{Binding EditarSocio}"
                            Grid.Column="1"
                            Content="Modificar"
                            Height="38"
                            Background="#FEF3C7"
                            BorderBrush="#F59E0B"
                            Margin="5"/>

                    <Button x:Name="btnEliminar"
                            Command="{Binding EliminarSocio}"
                            Grid.Column="2"
                            Content="Eliminar"
                            Height="38"
                            Background="#FEE2E2"
                            BorderBrush="#EF4444"
                            Margin="5"/>
                </Grid>

            </Grid>

        </Border>

    </Grid>
</Window>
